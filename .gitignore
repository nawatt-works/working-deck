.DS_Store
__pycache__/

# Every direct child is an independent Git repository or linked worktree.
# Keeping the entire area ignored prevents accidental gitlinks in the root repo.
repos/*
!repos/.gitkeep
