.DS_Store
__pycache__/

# repos/ is the default location for independent work repositories.
# Ignoring it prevents accidental gitlinks in the root repository.
repos/*
!repos/.gitkeep

# Repositories outside repos/ must be ignored by exact workspace-relative path.
# Examples:
# /clients/acme/legacy-api/
# /internal-tools/release-cli/
