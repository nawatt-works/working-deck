# Search tools should see work repositories even when the root Git ignores them.
# Using * rather than ** preserves each nested repository's own ignore rules.
!repos/*

# Add exact negations for registered repositories outside repos/ when needed.
# Examples:
# !/clients/acme/legacy-api/
# !/internal-tools/release-cli/
