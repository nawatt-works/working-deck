schema_version: 1
default_class: client

# Register every work repository with its workspace-relative path and class.
# Paths may live anywhere below the workspace except _Mission-Control/.
# A discovered but unregistered repository is treated as client and reported as an error.
repositories: []
